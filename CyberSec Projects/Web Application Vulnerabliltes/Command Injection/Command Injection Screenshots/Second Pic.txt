As seen in the second screenshot we have implemented a command injection into this web application.

We have gotten the web application to populate the current working directory.

Now this is a good start but what else can we do with command injection?
