Seen in the second screenshot we have implement a command injection into this web application.

We got have gotten the web application to give us back the current working directory.

Now this is a good start but what else can we do with command injection.